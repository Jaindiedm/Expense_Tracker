server {
    listen 80;

    # Serve React SPA — all routes go to index.html
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This makes React Router work — unknown paths serve index.html
        try_files $uri $uri/ /index.html;
    }
}